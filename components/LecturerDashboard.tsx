import React from 'react';

// This component is not currently used, but is kept to avoid breaking potential future imports.
const LecturerDashboard: React.FC = () => {
  return null;
};

export default LecturerDashboard;
